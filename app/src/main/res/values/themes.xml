<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.UnivacitiProject" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.UnivacitiProject" parent="Base.Theme.UnivacitiProject" />

    <style name="DialogAnimation">
        <item name="android:windowEnterAnimation">@anim/slidein_bottom</item>
        <item name="android:windowExitAnimation">@anim/slideout_bottom</item>
    </style>
</resources>